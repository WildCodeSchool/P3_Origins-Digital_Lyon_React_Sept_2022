# Dockerfile backend
FROM node:18.13

WORKDIR /usr/src/app

ARG APP_PORT=${APP_PORT}
ENV APP_PORT=${APP_PORT}
ARG FRONTEND_URL=${FRONTEND_URL}
ENV FRONTEND_URL=${FRONTEND_URL}
ARG DB_HOST=${DB_HOST}
ENV DB_HOST=${DB_HOST}
ARG DB_PORT=${DB_PORT}
ENV DB_PORT=${DB_PORT}
ARG DB_USER=${DB_USER}
ENV DB_USER=${DB_USER}
ARG DB_PASSWORD=${DB_PASSWORD}
ENV DB_PASSWORD=${DB_PASSWORD}
ARG DB_NAME=${DB_NAME}
ENV DB_NAME=${DB_NAME}
ARG JWT_SECRET=${JWT_SECRET}
ENV JWT_SECRET=${JWT_SECRET}
ARG JWT_TIMING=${JWT_TIMING}
ENV JWT_TIMING=${JWT_TIMING}
ARG AVATAR_DIRECTORY=${AVATAR_DIRECTORY}
ENV AVATAR_DIRECTORY=${AVATAR_DIRECTORY}

COPY ./backend .

RUN npm install

CMD [ "npm", "start" ]